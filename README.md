# Exasol Coding Challenge

## config.go and key.pem are not added here since they are sensitive files

### config.go template
```
package main

const (
	HOST      = "18.x.x.x"
	PORT      = x
	CERT_FILE = "key.pem"
)